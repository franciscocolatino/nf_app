<div id="<%= dom_id user %>" class="card mb-3">
  <div class="card-body">
    <p class="card-text">
      <strong>Nome:</strong>
      <%= user.name %>
    </p>

    <p class="card-text">
      <strong>Usuário:</strong>
      <%= user.username %>
    </p>

    <p class="card-text">
      <strong>Senha (hash):</strong>
      <%= user.password_digest %>
    </p>
  </div>
</div>
